import {ui} from "@playkit-js/kaltura-player-js";
import {h} from 'preact';

export class customMarker extends ui.preact.Component {
  render(props: any) {
    return <div {...props} />;
  }
}

export class customPreview extends ui.preact.Component {
  render(props: any) {
    return <div {...props} />;
  }
}

export class customSeekbarPreview extends ui.preact.Component {
  render(props: any) {
    return <div {...props} />;
  }
}
